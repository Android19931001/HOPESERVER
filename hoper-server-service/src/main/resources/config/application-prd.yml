spring:
  datasource:
    url: jdbc:mysql://10.102.100.132:3306/fls_dev?useUnicode=true&characterEncoding=utf8&useSSL=false&useLegacyDatetimeCode=false
    username: fls_dev
    password: Fls_dev@123
    driver-class-name: com.mysql.jdbc.Driver
  application:
    name: hope-service-app
  cloud:
    discovery:
      enabled: true
server:
  port: 4671
eureka:
  client:
    service-url:
      defaultZone: http://hopeuser:hopepassword123@mireureka:4670/eureka/
    instance:
      prefer-ip-address: true
      instance-id: hope-service-client
    fetch-registry: true
    register-with-eureka: true

mybatis-plus:
  mapper-locations: classpath:mapper/**/*.xml
  typeAliasesPackage: com.hope.service.*.domain
